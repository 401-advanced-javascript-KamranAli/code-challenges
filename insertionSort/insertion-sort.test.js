describe('last 6 weeks of code challenges', () => {
  it('insertion sort', () => {
    const arr = [4, 1, 3, 6, 2, 5];
    const result = insertionSort(arr);
    expect(result).toEqual([1, 2, 3, 4, 5, 6]);
  });


});